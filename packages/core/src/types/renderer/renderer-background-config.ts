// Types
import type { RendererColor } from '@/types/renderer';

/**
 * Настройки фона рендерера
 *
 * @public
 */
export type RendererBackgroundConfig = {
  color: RendererColor;
  transparent: boolean;
};
